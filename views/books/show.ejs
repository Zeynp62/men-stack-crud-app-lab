<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= book.bookName %></title>
  </head>
  <body>
    <a href="/books/">Back to Books</a>
    <h1><%= book.bookName %></h1>
    <p><%= book.author %></p>
    <% if (book.isRead) { %>
    <p>You finished reading this book</p>
    <% } else { %>
    <p>This book isn't finished yet</p>
    <% } %>
    <form action="/books/<%=book._id%>?_method=DELETE" method="POST">
      <button type="submit">Delete <%= book.bookName %></button>
      <a href="/books/<%= book._id %>/edit">Edit <%= book.bookName %></a>
    </form>
  </body>
</html>
